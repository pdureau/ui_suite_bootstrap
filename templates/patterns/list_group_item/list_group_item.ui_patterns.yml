list_group_item:
  label: "(List Group Item)"
  description: "Internal: to be used in the 'List Group' component. https://getbootstrap.com/docs/5.2/components/list-group/"
  category: "List group"
  variants:
    primary:
      label: "Primary"
    secondary:
      label: "Secondary"
    success:
      label: "Success"
    danger:
      label: "Danger"
    warning:
      label: "Warning"
    info:
      label: "Info"
    light:
      label: "Light"
    dark:
      label: "Dark"
  settings:
    active:
      type: "boolean"
      label: "Active?"
      description: "Indicate the current active selection."
      preview: false
      allow_expose: true
      allow_token: true
    disabled:
      type: "boolean"
      label: "Disabled?"
      description: "Make it appears disabled. Note that some elements with .disabled will also require custom JavaScript to fully disable their click events (e.g., links)."
      preview: false
      allow_expose: true
      allow_token: true
    url:
      type: "url"
      label: "URL"
      description: "The button URL. Optional."
  fields:
    content:
      type: "render"
      label: "Content"
      description: "Plain text, or any kind of content."
      preview: "An item"
