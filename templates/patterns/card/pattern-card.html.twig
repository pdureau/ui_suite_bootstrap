<div class="card">
  {% if image and setting_image_position != 'bottom' %}
    {{ image }}
  {% endif %}
  {{ include('@ui_patterns_bootstrap/patterns/card/_card-content.html.twig') }}
  {% if image and setting_image_position == 'bottom' %}
    {{ image }}
  {% endif %}
</div>
